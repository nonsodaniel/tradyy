import type { Metadata } from 'next';
import { Geist, Geist_Mono } from 'next/font/google';
import { ThemeProvider } from 'next-themes';
import './globals.css';

const geistSans = Geist({ variable: '--font-geist-sans', subsets: ['latin'] });
const geistMono = Geist_Mono({ variable: '--font-geist-mono', subsets: ['latin'] });

export const metadata: Metadata = {
  title: 'Tradyy — Global Market Intelligence',
  description:
    'Real-time prices, currency conversion, financial analytics, and portfolio tracking for global assets.',
  keywords: ['stocks', 'crypto', 'forex', 'portfolio', 'markets', 'finance'],
  authors: [{ name: 'nonsodaniel' }],
  openGraph: {
    title: 'Tradyy — Global Market Intelligence',
    description: 'Real-time financial data platform',
    type: 'website',
  },
};

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body className={`${geistSans.variable} ${geistMono.variable}`}>
        <ThemeProvider attribute="class" defaultTheme="dark" enableSystem>
          {children}
        </ThemeProvider>
      </body>
    </html>
  );
}
